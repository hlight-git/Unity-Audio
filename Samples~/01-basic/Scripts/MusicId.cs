namespace Hlight.Audio.Samples.Basic
{
    /// <summary>Your game declares this, not the package. Keep values contiguous from zero.</summary>
    public enum MusicId
    {
        Menu,
        Battle,
    }
}
